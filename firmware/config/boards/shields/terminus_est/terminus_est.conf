# Configuration for Terminus Est keyboard

# Enable split keyboard support
CONFIG_ZMK_SPLIT=y

# Increase BLE TX power for better range
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Enable deep sleep
CONFIG_ZMK_SLEEP=y